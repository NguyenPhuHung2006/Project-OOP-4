package network;

/**
 * Represents the possible responses a client can receive from the server
 * when attempting to establish a connection.
 */
public enum ConnectionResponse {
    ACCEPTED,
    REJECTED
}
